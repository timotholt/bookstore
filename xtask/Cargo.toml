[package]
name = "xtask"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlx = { version = "0.8", features = ["runtime-tokio", "postgres", "migrate", "tls-rustls"] }
tokio = { version = "1", features = ["rt"] }
serde_json = "1.0"
